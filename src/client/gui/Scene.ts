import { Component } from "engine/shared/component/Component";

export class Scene extends Component {
	/*
	private readonly shown = new ArgsSignal();
	private readonly hidden = new ArgsSignal();

	protected onShow(func: () => void) {
		this.shown.Connect(func);
	}
	protected onHide(func: () => void) {
		this.hidden.Connect(func);
	}

	show() {
		this.shown.Fire();
	}
	hide() {
		this.hidden.Fire();
	}
	*/
}
